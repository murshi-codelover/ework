import 'package:flutter/material.dart';

class ABM extends StatelessWidget {
  const ABM({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('A B M P A G E'),
      ),
    );
  }
}
